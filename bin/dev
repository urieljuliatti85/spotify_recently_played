#!/usr/bin/env sh
# Runs Rails, the Vite dev server (React HMR) and Solid Queue (the recurring
# Spotify sync) together — see Procfile.dev.
if ! gem list foreman -i --silent; then
  echo "Installing foreman..."
  gem install foreman
fi

exec foreman start -f Procfile.dev "$@"
